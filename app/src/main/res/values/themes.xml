<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CoffeeApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="appBarLayoutStyle">@style/CustomAppBarLayout</item>
        <item name="toolbarStyle">@style/CustomToolBar</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="CustomTextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <!-- border color -->
        <item name="boxStrokeColor">@color/coffee_500</item>
        <item name="boxStrokeWidth">2dp</item>
        <item name="hintTextColor">@color/coffee_500</item>
        <item name="android:textColorHint">@color/coffee_500</item>
        <item name="boxCornerRadiusBottomStart">100dp</item>
        <item name="boxCornerRadiusBottomEnd">100dp</item>
        <item name="boxCornerRadiusTopStart">100dp</item>
        <item name="boxCornerRadiusTopEnd">100dp</item>
    </style>
    <style name="CustomButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:layout_height">64dp</item>
        <item name="android:textColor">@color/cream</item>
        <item name="backgroundTint">@color/coffee_700</item>
        <item name="cornerRadius">100dp</item>
    </style>
    <style name="CustomAppBarLayout" parent="Widget.Design.AppBarLayout">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:stateListAnimator">@null</item>
    </style>
    <style name="CustomToolBar" parent="Widget.MaterialComponents.Toolbar">
        <item name="titleCentered">true</item>
        <item name="titleTextColor">@color/coffee_500</item>
        <item name="titleTextAppearance">@style/CustomTextAppearance</item>
    </style>
    <style name="CustomTextAppearance" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textStyle">bold</item>
    </style>
</resources>